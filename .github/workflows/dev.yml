name: Deployment

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: 
      name: production
    steps:
      - name: deploy
        run: ls
