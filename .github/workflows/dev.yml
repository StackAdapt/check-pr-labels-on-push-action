name: Deployment

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: 
      name: dev
    steps:
      - name: secret
        run: echo ${{ secrets.PROD_TOKEN }}

      - name: deploy
        run: ls
